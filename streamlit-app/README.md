# streamlit-app
Streamlit companion for Smart Scraper reusing the shared scraper core.
## Local
```bash
pip install -r requirements.txt
streamlit run app.py
```
## Deploy
Use the included Dockerfile/Procfile or deploy the folder on Streamlit Cloud.
