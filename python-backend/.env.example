# Copy to .env for local FastAPI development
PORT=8000
REQUEST_TIMEOUT=18
ALLOWED_ORIGINS=*

# Optional shared secret between your Netlify function and this backend
API_KEY=change-me-to-a-strong-random-string

# You can leave this empty for local runs
# PYTHON_API_URL is only used by Netlify, not here