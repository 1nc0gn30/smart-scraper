# Netlify environment variables
# Set in Netlify Dashboard > Site configuration > Environment variables

PYTHON_API_URL=https://your-backend.up.railway.app   # <-- point to your deployed backend
PYTHON_API_KEY=change-me-to-match-backend-api-key

# After deploying backend, also set ALLOWED_ORIGINS on the backend side to your Netlify URL